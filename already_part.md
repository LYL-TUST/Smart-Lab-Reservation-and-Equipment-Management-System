# 已完成部分

## 1. 项目规划已整理
已将 AI 小助手改造方案写入 `development_project.md`，明确了：
- 项目定位
- 半自动预约方案
- 页面形态（左侧菜单主入口 + 右下角悬浮快捷入口）
- Node + Express AI 服务与原 Spring Boot 业务后端分离的架构
- 半自动预约交互流程
- AI 服务接口清单

## 2. AI 小助手主入口页面已补充
新增并完善了 `frontend/src/views/AiAssistantPage.vue`，包含：
- AI 小助手主页面说明
- 快捷入口卡片
- 功能说明卡片
- “打开聊天窗”交互
- 使用说明步骤
- 与悬浮聊天窗联动的入口事件
- 会话历史说明
- 推荐交互流程展示（提出需求 → AI 追问 → 生成草稿 → 确认提交）

## 3. 右下角悬浮 AI 聊天窗已完善
完善了 `frontend/src/components/AiAssistant.vue`，实现：
- 右下角悬浮按钮
- 右侧抽屉聊天窗
- 常用问题快捷按钮
- 聊天消息展示
- 预约草稿展示
- 可预约资源展示
- 草稿确认提交
- 预约结果卡片展示
- AI 建议补充信息展示
- 会话历史折叠面板
- 从页面入口或外部事件打开聊天窗
- 支持保留会话上下文
- 打字中反馈与消息时间戳

## 4. 前端布局与菜单入口已接入
已确认并保留左侧菜单中的 `AI 小助手` 路由入口，形成：
- 左侧菜单主入口
- 右下角悬浮快捷入口
两种入口并行的产品结构。

## 5. AI 服务端已搭建
已建立 `ai-server/` 独立 Node + Express 服务，并补充了基础接口：
- `GET /api/ai/health`
- `GET /api/ai/suggestions`
- `GET /api/ai/session/:sessionId`
- `POST /api/ai/chat`
- `POST /api/ai/reservation/draft`
- `POST /api/ai/reservation/confirm`
- `POST /api/ai/resources/available`
- `POST /api/ai/intent/parse`
- `POST /api/ai/faq`
- `POST /api/ai/conversation/reset`
- `DELETE /api/ai/conversation/reset`

## 6. AI 服务能力已增强
`ai-server/src/index.js` 已增加这些能力：
- 会话历史记录
- 更完整的意图识别
- 预约请求识别
- 资源查询
- 草稿生成
- 追问机制
- FAQ 常见问答
- 预约确认返回结果
- 会话重置能力
- 对接硅基流动 DeepSeek 的结构化输出解析
- 大模型返回失败时的本地 fallback 逻辑

## 7. 前后端联动已开始
当前前端与 AI 服务已经开始形成联动：
- 页面可以触发打开聊天窗
- 聊天窗会请求 AI 服务
- AI 服务可返回草稿和资源结果
- 支持半自动预约的交互雏形
- 主入口页面可通过快捷卡片预填问题并打开聊天窗
- 前端会自动携带登录 token 调用 `ai-server`
- `ai-server` 会把 token 透传给 Spring Boot 后端

## 8. 真实后端数据已打通验证
已经通过 Apifox 验证了 Spring Boot 后端的真实实验室数据接口：
- `/api/laboratory/page` 能返回真实实验室列表
- 响应结构为 `data.records`
- 实验室状态使用英文枚举，如 `IDLE`、`RESERVED`、`MAINTENANCE`
- `ai-server` 已调整为按真实结构读取并过滤资源

## 9. 代码质量检查已完成
对当前改动涉及的主要文件执行了 lint 检查，结果显示：
- 没有新增 lint 错误

## 10. 在执行过程中遇到的问题与解决方法

### 问题 1：左侧菜单中看不到 AI 小助手入口
**原因：** 路由入口虽然已经添加，但菜单显示受权限过滤影响，当前账号未必满足 `DASHBOARD_VIEW` 权限。

**解决方法：**
- 将 `AI 小助手` 作为独立页面入口保留在路由中
- 调整页面设计，不再依赖仅限仪表盘权限的显示逻辑
- 让主入口页面作为 AI 功能的明确承载页

### 问题 2：右下角悬浮按钮不明显或疑似未显示
**原因：** 悬浮入口最初挂载在 `App.vue`，在复杂布局中容易受到层级和容器影响，导致显示不稳定。

**解决方法：**
- 将悬浮 AI 按钮改为挂载在 `MainLayout.vue`
- 保证它只在登录后的主布局中显示
- 提高入口稳定性，避免被页面结构遮挡

### 问题 3：AI 聊天体验过于基础，缺少草稿和资源结果展示
**原因：** 初始版本只支持简单的文本问答，未把预约业务中的草稿和资源推荐信息展示出来。

**解决方法：**
- 在 `AiAssistant.vue` 中增加资源推荐区块
- 增加预约草稿展示与确认按钮
- 让 AI 服务返回结构化数据，前端按字段渲染

### 问题 4：AI 服务看起来不像真实服务，只是简单 mock
**原因：** 初版 `ai-server` 只返回固定文本，缺少会话历史、意图识别和追问机制。

**解决方法：**
- 增加会话历史记录 `sessions`
- 增加更完整的意图识别函数 `detectIntent`
- 增加字段抽取和追问逻辑 `extractFields`
- 增加 `faq`、`conversation/reset`、`intent/parse` 等接口
- 后续接入硅基流动 DeepSeek，让 AI 回答更自然

### 问题 5：页面和聊天窗之间联动不够自然
**原因：** 主页面和悬浮聊天窗之间没有统一的打开方式。

**解决方法：**
- 在 `AiAssistant.vue` 中监听外部事件 `open-ai-assistant`
- 在 `AiAssistantPage.vue` 中通过快捷卡片触发该事件
- 实现“从页面直接打开聊天窗”的联动效果

### 问题 6：实验室数据查不到
**原因：** `ai-server` 早期读取后端实验室数据时，接口路径、返回结构和状态枚举没有对齐。

**解决方法：**
- 先用 Apifox 验证 Spring Boot 返回的真实数据结构
- 确认接口路径为 `/api/laboratory/page`
- 根据 `data.records` 读取实验室列表
- 按 `IDLE` 状态过滤可预约资源
- 在 `ai-server` 中加入 token 透传，解决 401 问题

### 问题 7：调用后端接口返回 401 未认证
**原因：** `ai-server` 请求 Spring Boot 业务接口时没有携带用户 token。

**解决方法：**
- 前端在请求 `ai-server` 时自动携带登录 token
- `ai-server` 再将 `Authorization` 头透传给 Spring Boot
- 这样资源查询和其他业务接口就能通过认证

### 问题 8：浏览器直接打开资源查询接口返回 `Cannot GET`
**原因：** 浏览器地址栏默认发出 GET 请求，而资源接口原本是 POST。

**解决方法：**
- 用 Apifox 或前端请求以 POST 调用该接口
- 必要时再补充 GET 版本用于调试

### 问题 9：AI 仍然没有拿到真实数据
**原因：** 经过进一步排查，Spring Boot 后端的实验室数据本身是正常的，但 `ai-server` 在转发请求时还需要确认 token 透传与接口读取是否完全一致；同时实验室状态字段是英文枚举 `IDLE / RESERVED / MAINTENANCE`，筛选逻辑必须与之对齐。

**解决方法：**
- 在前端 `frontend/src/api/ai.js` 中自动为 AI 请求带上登录 token
- 在 `ai-server` 中把前端传入的 `Authorization` 继续透传给 Spring Boot
- 资源查询改为读取 `/api/laboratory/page?current=1&size=50` 的 `data.records`
- 仅保留 `status === IDLE` 的实验室作为可预约资源
- 用 `ai-server` 日志打印 `Authorization` 预览值，继续排查是否真实传到后端
- 如仍为空，再结合 Spring Boot 日志继续确认是否 401 或路径不匹配

## 当前状态总结
目前项目已经完成了 AI 小助手改造的核心骨架，具备：
- 一个独立的 AI 主入口页面
- 一个右下角悬浮快捷入口
- 一个独立的 Node + Express AI 服务
- 半自动预约交互的初步闭环
- 与原业务系统并行的架构基础
- 已识别并解决了入口显示、布局挂载、联动体验、数据解析和认证透传等问题
- 已接入硅基流动 DeepSeek 的阶段性能力

## 11. DeepSeek / 硅基流动联调过程已继续推进
在后续联调中，进一步确认了 `ai-server` 已经能够真正进入模型调用分支，并向硅基流动平台发起请求，但最初持续返回 `401 Api key is invalid`。随后又通过对照 Apifox 成功请求、硅基流动官方示例以及日志排查，逐步确认：
- `ai-server` 的请求确实已经发出，并非“没有调用大模型”
- 前端到 `ai-server` 的链路是通的
- `ai-server` 到硅基流动的链路也已经打通到请求级别
- 但平台侧仍然返回认证错误，导致没有真实扣费，也没有返回有效模型输出

### 进一步排查结论
- 通过 `ai-server` 日志确认：
  - 已进入 model branch
  - 已发起 `POST https://api.siliconflow.cn/v1/chat/completions`
  - 已带上 `Authorization` 和 `X-API-Key`
  - 仍返回 `401 Api key is invalid`
- 用户随后说明该 API Key 在 Apifox 中是可以正常通过的，说明：
  - key 本身大概率有效
  - 问题更可能出在 `ai-server` 与硅基流动之间的请求格式、模型名或权限范围差异
- 进一步对照硅基流动官方示例后，将模型名尝试调整为官方示例中可通的模型：`Pro/zai-org/GLM-4.7`
- 目前问题尚未完全解决，但已经明确：
  - 不是前端问题
  - 不是 Spring Boot 业务后端问题
  - 不是 `ai-server` 没有发请求
  - 而是硅基流动平台对当前调用参数/模型组合仍拒绝认证

## 12. 资源查询、推荐理由与前端展示已增强
在资源推荐链路上，已经完成以下优化：
- `ai-server` 读取真实实验室分页数据 `data.records`
- 按 `IDLE` 状态筛选可预约实验室
- 按人数和设备需求进行推荐排序
- 为每个推荐项生成 `reason` 推荐理由
- 前端聊天卡片中展示“推荐理由”
- 前端资源卡片中展示“为什么推荐它”以及推荐等级标签
- DeepSeek 的回复提示被进一步调整为更自然的解释风格，而不是只列实验室名字

## 当前状态总结
目前项目已经完成了 AI 小助手改造的核心骨架，具备：
- 一个独立的 AI 主入口页面
- 一个右下角悬浮快捷入口
- 一个独立的 Node + Express AI 服务
- 半自动预约交互的初步闭环
- 与原业务系统并行的架构基础
- 已识别并解决了入口显示、布局挂载、联动体验、数据解析和认证透传等问题
- 已接入并尝试联通硅基流动 DeepSeek / 其他模型调用
- 已明确大模型联调中存在“API Key / 模型权限 / 请求格式”差异问题，仍在继续排查

## 下一步可继续推进的内容
- 继续增强大模型调用的稳定性与容错能力
- 彻底确认硅基流动请求格式与可用模型名
- 将 AI 草稿与真实业务数据进一步打通
- 优化可预约资源的排序和推荐策略
- 增强前端聊天窗的追问按钮和确认卡片
- 将 AI 主页面做得更像真正产品首页
- 完善会话历史展示与多轮上下文体验
- 继续排查 AI 中间层与业务后端之间的认证透传与资源读取细节

## 13. 本轮对话补充：当前建议的优先级
基于现有文档内容，当前最值得优先推进的是“AI 输出稳定性 + JSON 容错”，原因如下：
- 后续草稿、资源推荐、追问和确认提交都依赖稳定的结构化输出
- 不稳定的模型返回会直接影响半自动预约闭环
- 先把输出格式稳住，再做体验和推荐策略优化，整体推进效率更高

### 推荐的下一步顺序
1. 增强 AI 输出稳定性，完善结构化 JSON 解析与 fallback 逻辑
2. 继续打通 AI 草稿与真实业务数据的对应关系
3. 优化可预约资源推荐排序与推荐理由
4. 完善聊天窗交互，包括追问、确认卡片和会话历史展示

## 15. 本轮对话补充：草稿时间、提交校验与前端体验优化
在后续联调中，又进一步完善了预约草稿与提交交互，主要解决“草稿不完整仍可提交、时间显示过旧、失败提示不友好”等问题。

### 问题 1：草稿时间落到过去日期，容易导致提交失败
**现象：** AI 生成的草稿里偶尔会出现过期时间（例如历史年份），一点击提交就被后端拒绝。

**解决方法：**
- 在 `ai-server` 里增加草稿时间归一化逻辑
- 如果草稿开始时间明显早于当前时间，就自动修正到未来时间
- 对“下周 / 本周 / 这周 / 周X”的时间表达做了更准确的解析
- 在没有解析出具体时间时，额外给出一个“未来最近可用时间”建议

### 问题 2：草稿不完整时仍然显示“确认提交”按钮
**现象：** 当草稿缺少实验室名称或时间信息时，前端仍可能让用户直接提交，最终返回 400。

**解决方法：**
- 前端只在草稿完整时显示“确认提交”按钮
- 草稿不完整时只展示追问信息，不允许误提交
- 在草稿卡片里显式提示缺失字段，例如“还缺实验室名称”“还缺预约时间”
- 提高用户对草稿状态的可见性，减少误操作

### 问题 3：`confirm` 失败提示不够友好
**现象：** 后端返回了 400，但前端提示过于笼统，不知道具体是哪一步失败。

**解决方法：**
- 前端将 `confirm` 的失败原因映射成更友好的中文提示
- 例如：
  - `预约时间已过期，请选择未来时间后再提交。`
  - `该时间段已被占用，请更换时间或实验室。`
  - `实验室信息不完整，请先补全实验室名称、时间等信息。`
  - `当前登录已失效，请重新登录后再试。`
- 如果识别不到具体原因，则回退到原始错误信息，方便继续排查

### 问题 4：模型输出带 Markdown 代码块，直接解析会报错
**现象：** 硅基流动返回的 JSON 外层包着 ```json 代码块，直接 `JSON.parse()` 会失败。

**解决方法：**
- 在 `ai-server` 中增加 `normalized` 清洗逻辑
- 去掉 ```json 和结尾 ``` 后再解析
- 支持纯 JSON 和 Markdown 包裹 JSON 两种返回形式

### 问题 5：前端请求超时，体验不稳定
**现象：** 某些 `chat` 请求会在 15 秒内超时，导致前端显示“请求失败”。

**解决方法：**
- 将前端 AI 请求超时时间从 15 秒提高到 45 秒
- 在 `ai-server` 里增加耗时日志，打印：
  - `model branch elapsed=...ms`
  - `resource_query backend elapsed=...ms`
  - `chat total elapsed=...ms`
- 这样后续可继续定位是模型慢、资源查询慢，还是整体链路偏慢

### 问题 6：草稿生成与提交体验需要更像真实产品
**现象：** 早期版本更多像“能用的原型”，还不够像一个成熟的预约助手。

**解决方法：**
- 资源和追问可以同时展示，不再互相遮蔽
- 资源卡片中展示推荐理由
- 草稿卡片里展示缺失字段与追问建议
- 按状态显示不同的提交按钮和结果卡片
- 整体交互更接近“先生成草稿，再确认提交”的真实业务闭环

### 本轮联调的最终确认
当前项目已确认具备：
- 真实模型调用能力
- 真实后端数据查询能力
- JWT 鉴权透传能力
- 草稿生成与追问能力
- 草稿完整性校验能力
- 更友好的提交失败提示
- 更贴近真实产品的预约交互体验

### 这轮优化带来的项目价值
- 大幅降低了“草稿不完整误提交”的概率
- 把时间解析、草稿修正、失败提示做得更贴近真实业务
- 让 AI 助手从“能聊天”进一步变成“能辅助完成预约流程”
- 为后续更真实的确认提交、冲突校验和推荐排序打下了基础

## 14. 本轮对话补充：重要踩坑点与最终确认结果
本轮联调过程中，针对 AI 服务、硅基流动调用、后端鉴权和实验室数据读取，已经把关键坑点逐一排清，最终结论如下。

### 坑点 1：硅基流动一直返回 `401 Api key is invalid`
**现象：** `ai-server` 请求硅基流动时持续返回 401，看起来像是 key 失效。

**排查过程：**
- 增强 `ai-server` 日志，打印了请求头预览、请求体预览、模型返回原文
- 先后验证了 `Authorization`、`X-API-Key`、`stream: false` 等兼容参数
- 检查了 `.env`、启动日志和环境变量来源
- 发现最初报错并不是“没发请求”，而是平台侧拒绝了当前调用组合

**最终结论：**
- 硅基流动侧 key 在某些配置下可用，但调用参数和模型组合可能不匹配
- 在当前项目中，`deepseek-ai/DeepSeek-V4-Flash` 与官方 OpenAI 兼容调用方式最终可正常返回
- 401 问题已经从“前端/中间层没发请求”定位为“平台侧鉴权/模型组合差异”

### 坑点 2：`.env` 里的变量看起来没生效
**现象：** 启动日志里一度打印出旧模型名 `Pro/zai-org/GLM-4.7`，让人怀疑 `.env` 没读到。

**排查过程：**
- 为 `ai-server` 增加了 boot config、env source、raw env、trimmed、whitespace 等日志
- 明确了 `process.env.DEEPSEEK_MODEL` 的原始值与最终值
- 发现问题不是 `.env` 文件不存在，而是运行时值与预期不一致
- 通过 `dotenv.config({ override: true })` 强制让 `.env` 覆盖系统环境变量

**最终结论：**
- `.env` 可以覆盖系统环境变量，但前提是显式启用 `override: true`
- 这一步解决了“运行时模型值不符合预期”的问题

### 坑点 3：模型返回的是 Markdown 代码块包裹的 JSON
**现象：** 模型虽然返回了结构化内容，但外层包了一层 ```json 代码块，导致 `JSON.parse()` 报错。

**排查过程：**
- 打印模型返回原文后，发现内容是：
  - 开头 ```json
  - 中间 JSON
  - 结尾 ```
- 原始 `JSON.parse(raw)` 无法直接解析

**最终解决：**
- 在 `ai-server` 中加入 `normalized` 清洗逻辑
- 去掉开头的 ```json 和结尾的 ``` 后再解析
- 这样可兼容纯 JSON、代码块 JSON，以及大模型常见的轻微格式波动

### 坑点 4：资源查询一度显示 `records=0`
**现象：** `ai-server` 资源查询日志中出现 `records=0`，一度怀疑后端没有实验室数据。

**排查过程：**
- 先确认前端到 `ai-server` 的 token 透传
- 再确认 `ai-server` 到 Spring Boot 的 `Authorization` 透传
- 后端日志中先出现 `JWT校验结果: valid=false`，说明 token 未通过认证
- 继续排查后发现：后端分页接口本身是有数据的，只是前一阶段由于认证失败，接口返回了 401
- 进一步联调后，`ai-server` 成功拿到了 `resource_query backend status=200 ok=true`，并打印出 `records=15`

**最终结论：**
- `records=0` 的根因并不是数据库空，而是前一阶段认证没通过，导致后端返回 401
- 当 token 校验恢复正常后，`/api/laboratory/page` 能返回真实实验室列表，且 `data.records` 里有 15 条数据

### 坑点 5：后端 `JWT校验结果: valid=false`
**现象：** Spring Boot 收到了 token，但 `JwtUtil.validateToken(token)` 返回 `false`。

**排查过程：**
- 在 `JwtAuthenticationFilter` 中增加了认证日志，确认请求头和 token 预览值
- 在 `JwtUtil.validateToken()` 中把失败原因从大段异常日志改为更轻量的 debug 原因
- 发现最终有效 token 可以通过校验，`JWT校验结果: valid=true`

**最终结论：**
- 认证链路要求 `Authorization: Bearer <token>`
- token 必须是当前后端 `jwt.secret` 签发的有效 JWT
- 当 token 有效后，后端会放行 `/laboratory/page`，资源查询就能正常返回

### 坑点 6：`ai-server` 打印日志太多，影响排查效率
**现象：** 在多轮调试中，日志一度过于冗长，阅读成本较高。

**处理方式：**
- 增加关键节点日志，但避免无意义重复输出
- 删除 `JwtAuthenticationFilter` 中未使用的引用
- 将 token 校验失败原因改为更简洁的 debug 输出

**最终结论：**
- 保留“足够定位问题”的日志即可，避免把排查信息淹没在噪音里

### 本轮联调的最终状态确认
现在项目链路已经确认打通：
- 前端可调用 `ai-server`
- `ai-server` 可调用硅基流动模型并解析返回
- `ai-server` 可透传登录 token 到 Spring Boot
- Spring Boot 可通过 JWT 认证
- `ai-server` 可读取真实实验室分页数据 `data.records`
- `resource_query` 能拿到 15 条真实实验室记录

### 这轮联调对项目的实际价值
- 证明 AI 服务不是 mock，而是能真正接入模型和业务后端
- 证明半自动预约链路是可跑通的
- 为后续优化“追问、推荐理由、确认提交、失败解释”打下了真实数据基础
- 让项目从“看起来像 AI”变成“真的接上了 AI 和业务系统”
