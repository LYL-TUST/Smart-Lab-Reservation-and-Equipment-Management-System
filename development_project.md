# development_project

## AI 小助手改造规划

### 项目定位
在原有“智能实验室预约与设备管理平台”基础上，新增一个面向预约场景的 AI 小助手入口，让系统从传统管理平台升级为“业务平台 + 智能助手”的产品形态，增强作品在 AI 应用开发投递中的竞争力。

### 核心目标
- 保留原有预约、设备、课程、用户管理等核心业务能力
- 新增 AI 小助手入口，支持自然语言交互
- 让 AI 能查询预约数据、解释规则、推荐可预约资源
- 支持自然语言发起预约，并在确认后提交到现有业务系统

### 产品定位表达
建议对外描述为：
- 面向高校场景的智能实验室预约与设备管理平台
- 集成 AI 智能助手的实验室预约系统
- 支持自然语言问答、资源推荐与确认式自动预约

### 功能分阶段规划

#### 第一阶段：AI 入口与基础聊天
目标是先把 AI 能力“接入系统”，让用户可以在平台中直接与助手对话。

功能点：
- 在仪表盘或侧边导航增加 `AI 小助手` 入口
- 点击后打开聊天抽屉或独立页面
- 支持发送问题、接收文字回复
- 支持常用问题快捷入口
- 支持对话历史展示

这一阶段重点不是自动执行任务，而是完成交互形态和产品包装。

#### 第二阶段：业务问答与数据查询
让 AI 真正和预约业务数据结合起来。

AI 可回答的问题：
- 还有哪些实验室/教室可以预约
- 某个时间段是否空闲
- 某个实验室是否在维护中
- 我当前有哪些预约
- 预约规则和约束是什么
- 某个时间是否存在冲突

这一阶段的关键是：AI 通过后端接口查询真实数据，再用自然语言进行解释。

#### 第三阶段：自然语言预约草稿
让 AI 可以把用户口语化的预约请求，解析成结构化信息。

例如用户输入：
- 帮我预约下周三下午 2 点到 4 点的 301 实验室
- 我想预约一个能容纳 30 人、带投影仪的教室
- 帮我约一个周五上午可用的实验室

AI 需要完成：
- 意图识别
- 时间解析
- 场地解析
- 人数和设备需求识别
- 信息缺失时追问
- 生成预约草稿

#### 第四阶段：半自动预约
采用“半自动”而不是“全自动”方案：AI 先根据用户输入生成预约草稿，系统完成规则校验和冲突检测后，再由用户二次确认提交。

流程建议：
1. 用户提出预约需求
2. AI 解析并生成预约草稿
3. 系统检测冲突和规则限制
4. AI 展示摘要信息
5. 用户点击确认
6. 系统创建预约记录
7. 返回成功或失败结果

#### 半自动预约的具体交互流程
1. 用户点击右下角悬浮按钮，打开 AI 聊天窗
2. 用户用自然语言输入预约需求，例如“帮我预约下周三下午 2 点到 4 点的 301 实验室”
3. AI 识别意图，并提取时间、地点、人数、设备需求等信息
4. 如果信息不完整，AI 先追问补充，例如“请问您需要预约的具体实验室编号是什么？”
5. AI 调用 Node + Express 的业务编排接口，查询可用资源和冲突情况
6. Node 服务再转调 Spring Boot 业务接口，获取真实数据和校验结果
7. AI 返回一份预约草稿摘要，包括预约时间、资源名称、用途、冲突状态和注意事项
8. 用户点击“确认提交”按钮后，Node 服务调用 Spring Boot 的创建预约接口
9. Spring Boot 完成权限校验、冲突校验和数据库写入
10. 系统返回预约成功或失败结果，并在聊天窗中展示
11. 如果失败，AI 需要说明失败原因，并给出可替代的预约建议

#### 半自动方案的注意事项
- AI 不能直接绕过业务流程写数据库，所有创建、修改、取消都必须走 Spring Boot 业务接口
- 任何涉及时间、地点、人数、设备的预约操作，都要先做冲突校验
- 用户输入不完整时，AI 要先追问补全信息，而不是直接猜测提交
- 预约提交前必须展示摘要确认，避免误操作
- 如果 AI 判断不确定，优先返回“我需要你补充信息”，不要强行执行
- 对于权限敏感操作，例如取消他人预约、审批预约、修改课程预约，必须严格沿用原有权限控制
- Node + Express AI 服务只负责理解、编排和调用接口，不负责核心业务落库

### 推荐页面形态
采用两种入口形式并行：
- 左侧菜单新增 `AI 小助手`，作为主入口，方便在管理平台中长期使用
- 右下角悬浮按钮作为快捷入口，方便用户在任何页面快速唤起助手

这两种形式可以同时存在：
- 左侧菜单负责完整功能入口和页面承载
- 右下角悬浮按钮负责快速唤起聊天窗

推荐整体交互仍以“菜单入口 + 右侧抽屉”形式为主，右下角悬浮按钮作为辅助快捷入口，兼顾管理平台风格和使用效率。

### 推荐技术路线
#### 最终采用方案
- 前端继续使用 Vue 3
- 原有业务后端保留 Spring Boot，不做整体重构
- 新增一个独立的 `Node + Express` AI 服务
- AI 服务专门负责聊天、意图识别、自然语言解析、工具调用和预约草稿生成
- 业务数据查询、冲突校验、预约创建、审批等仍由原 Spring Boot 后端负责
- AI 服务通过接口调用业务后端，实现“AI 能力层”和“业务能力层”解耦

#### 为什么这样做
- 不破坏现有业务系统
- 开发成本更低，推进速度更快
- 便于你把重点放在 AI 能力和产品展示上
- 更适合投递 AI 应用开发类岗位
- 以后也方便继续扩展新的 AI 能力，例如推荐、追问、总结、语音输入等

#### 总体结构
- `Vue 前端`：展示页面、右下角 AI 悬浮按钮、聊天窗口、预约确认弹窗
- `Spring Boot 后端`：负责原有业务接口和数据库操作
- `Node + Express AI 服务`：负责智能问答、意图识别、工具编排和调用业务接口

### Node + Express AI 服务接口清单

#### 1. 聊天消息接口
- `POST /api/ai/chat`
- 作用：接收用户消息，返回 AI 回复
- 请求内容：用户输入、用户身份、会话 ID、上下文信息
- 返回内容：AI 文本回复、是否需要追问、是否生成预约草稿

#### 2. 会话历史接口
- `GET /api/ai/conversations/:conversationId`
- 作用：获取某个会话的历史消息
- 用途：用于页面重新打开时恢复上下文

#### 3. 可预约资源查询接口
- `POST /api/ai/availability/query`
- 作用：根据时间、人数、设备需求查询可预约实验室或教室
- 返回内容：可用资源列表、推荐排序、冲突说明

#### 4. 预约草稿生成接口
- `POST /api/ai/reservation/draft`
- 作用：将自然语言预约需求转成结构化草稿
- 返回内容：预约时间、资源、人数、用途、设备需求、待确认项

#### 5. 预约确认提交接口
- `POST /api/ai/reservation/confirm`
- 作用：用户确认后，由 AI 服务触发真正的预约创建流程
- 逻辑：Node 服务调用 Spring Boot 创建预约接口

#### 6. 预约失败解释接口
- `POST /api/ai/reservation/explain-failure`
- 作用：当预约失败时，输出人类可读的失败原因和替代建议

#### 7. 快捷问答接口
- `POST /api/ai/faq`
- 作用：回答预约规则、使用说明、权限说明等固定问题

#### 8. 推荐接口
- `POST /api/ai/recommend`
- 作用：根据用户条件推荐最合适的实验室或教室

#### 9. 追问信息补全接口
- `POST /api/ai/follow-up`
- 作用：当预约信息不完整时，返回需要用户补充的问题

#### 10. 会话上下文清理接口
- `DELETE /api/ai/conversations/:conversationId`
- 作用：清空某次会话上下文，重新开始新的对话

#### 接口设计原则
- AI 服务接口以“对话”和“编排”为主，不直接操作业务数据库
- 涉及预约创建、修改、取消、审批的动作，最终都必须落到 Spring Boot 业务接口
- 所有接口都要保留用户身份和权限信息，避免越权操作
- 聊天接口要兼容追问、草稿、确认、失败解释等多轮对话状态

#### 结论
不建议为了加 AI 直接整体重构整个业务后端，因为成本高、风险大，而且会拖慢项目完成进度。

### 设计原则
- AI 负责理解，不直接越权操作数据库
- 所有预约类操作必须经过规则校验
- 所有自动提交都必须有二次确认
- 先做业务闭环，再做炫技功能
- AI 必须和预约业务深度结合，避免做成普通聊天机器人

### 建议开发顺序
1. 增加 AI 小助手入口
2. 完成基础聊天 UI
3. 接入预约相关问答
4. 打通实验室空闲查询
5. 实现预约草稿生成
6. 增加确认式预约提交
7. 完善推荐、追问、失败解释等能力
8. 再考虑流式输出、上下文记忆、语音输入等增强功能

### 项目亮点包装
可用于简历或项目介绍：
- 面向高校场景的实验室预约管理平台
- 集成 AI 智能助手，实现自然语言查询与预约推荐
- 支持从问答到预约执行的完整业务闭环
- 结合业务规则校验与二次确认机制，保证自动化操作安全可靠

### 当前阶段建议
如果你想尽快把项目做成投递作品，建议优先完成：
- AI 入口
- 预约问答
- 可预约资源查询
- 自然语言预约草稿
- 确认式提交

这样可以最快形成一个“有 AI 特色且能演示完整流程”的版本。
